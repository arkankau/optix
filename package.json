{
  "name": "nearify-exam",
  "version": "1.0.0",
  "private": true,
  "description": "Voice-first subjective refraction exam MVP",
  "scripts": {
    "dev": "concurrently \"pnpm --filter @nearify/api dev\" \"pnpm --filter @nearify/web dev\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "@elevenlabs/elevenlabs-js": "^2.22.0",
    "dotenv": "^17.2.3"
  }
}

